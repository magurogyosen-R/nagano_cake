<headar>
  <nav class="navbar navbar-expand-lg navbar-right">
    <div class="container">
      <a class="navbar-brand p-3" href="/"><!--ここに画像を入れる--></a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav ml-auto">
        <% if customer_signed_in? %>
          <li>
            <%= link_to public_order_path,class: 'btn btn-sm btn-outline-secondary' do %>
              マイページ
            <% end %>
          </li>
          <li>
            <%=  link_to public_items_path,class: 'btn btn-sm btn-outline-secondary' do %>
              商品一覧
            <% end %>
          </li>
          <li>
            <%=  link_to public_cart_item_path ,class: 'btn btn-sm btn-outline-secondary' do %>
              カート
            <% end %>
          </li>
          <li>
            <%= link_to destroy_customer_session_path, method: :delete,class: 'btn btn-sm btn-outline-secondary' do %>
              ログアウト
            <% end %>
          </li>
        <% else %>
          <li>
            <%= link_to public_about_path,class: 'btn btn-sm btn-outline-secondary' do %>
              About
            <% end %>
          <li>
          <li>
            <%= link_to public_items_path,class: 'btn btn-sm btn-outline-secondary' do %>
              商品一覧
            <% end %>
          </li>
          <li>
            <%= link_to new_customer_registration_path,class: 'btn btn-sm btn-outline-secondary' do %>
              新規登録
            <% end %>
          </li>
          <li>
            <%= link_to new_customer_session_path ,class: 'btn btn-sm btn-outline-secondary' do %>
              ログイン
            <% end %>
          </li>
        <% end %>
        </ul>
      </div>
      <form class="d-flex">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-secondary" type="submit">Search</button>
      </form>
    </div>
  </nav>
</headar>